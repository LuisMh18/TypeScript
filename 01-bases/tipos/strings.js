"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const linternaVerde = "Linterna verde";
    const volcanNnegro = `Héroe: Volcan negro`;
    console.log(batman.toUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No está definido');
})();
